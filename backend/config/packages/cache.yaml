framework:
    cache:
        # Unique name of your app: used to compute stable namespaces for cache keys.
        #prefix_seed: your_vendor_name/app_name

        # The "app" cache stores to the filesystem by default.
        # The data in this cache should persist between deploys.
        # Other options include:

        # Redis
        #app: cache.adapter.redis
        #default_redis_provider: redis://localhost

        # APCu (not recommended with heavy random-write workloads as memory fragmentation can cause perf issues)
        #app: cache.adapter.apcu

        # setly-phase-exercise-media.md §2.5/§5: a dedicated Redis-backed pool
        # for GET /exercises filtered-list caching only — deliberately not
        # switching the app's default "app" pool (filesystem) to Redis, so
        # this stays the one thing that depends on the new Redis service.
        # ExerciseCollectionProvider reads it; ImportExercisesCommand clears
        # it wholesale on every successful import (setly-phase-exercise-
        # media.md §5: "invalidated only when the catalog is re-imported").
        default_redis_provider: '%env(REDIS_URL)%'
        pools:
            exercise_catalog.cache:
                adapter: cache.adapter.redis
